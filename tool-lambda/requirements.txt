# Core AWS dependencies (minimal)
boto3>=1.34.0

# Strands Agents framework - AI agent SDK (pinned to avoid opentelemetry issues)
strands-agents==1.4.0

# MCP (Model Context Protocol) support
mcp>=0.1.0
