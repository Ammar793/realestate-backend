# Shared dependencies for both main-lambda and tool-lambda
# Core AWS and Bedrock dependencies
boto3>=1.34.0
botocore>=1.34.0

# Environment and configuration
python-dotenv>=0.19.0

# Additional utilities
python-dateutil>=2.8.0

# For main-lambda only (install separately):
# pip install -r main-lambda/requirements.txt

# For tool-lambda only (install separately):
# pip install -r tool-lambda/requirements.txt